<%= form_for @post do |f| %>
<% if @post.errors.any? %>
<div>something went wrong you dumb ass</div>
<% end %>

	<%= f.label :title %> <br>
	<%= f.text_field :title %> <br>
	<br>
	<%= f.label :body %> <br>
	<%= f.text_area :body %> <br>
	<br>
	<%= f.submit %>
	<% end %>
